# 汉献帝之末路 v2.0.0 大修方案

> **弟子**：小凤雏 (Hermes Agent)
> **时间**：2026-06-01
> **基准版本**：v1.18.0 (commit 7685e2f)
> **目标版本**：v2.0.0
> **本文件**：待主公审批

---

## 〇、摸底（实测数据，不靠记忆）

| 维度 | 数量 |
|------|------|
| Python 后端 | 14,151 行 / 27 个模块 / db.py 2,757 行最大 |
| TypeScript 前端 | 5,098 行 / 22 个组件 / App.tsx 906 行最大 |
| CSS | 119.9 KB / 15 个文件 / app.css 45.8 KB 最大 |
| 测试 | 1,001 行 / 4 个 test_*.py + e2e_test.py / **83/83 通过** |
| .agno_skills | 16 技能 / 0.5-2.5 KB（偏小） |
| content/ 资产 | 20 JSON + 11 prompt / characters.json 100 KB |
| 服务 | HTTP 200 健康 / 5555 端口 / 进程跑了 16h+ |
| Git 提交 | 263 文件 / 26 commits（v0.5.0~v1.18.0） |

---

## 一、🚨 紧急 P0 bug（**5+10 个**，必修）

### P0-A 后端致命 bug（5 个）

| # | 位置 | 现象 | 验证 | 修复 |
|---|------|------|------|------|
| **P0-A1** | `simulation.py:603-607` | `db.save_state("turn", state.turn)` 传字符串，`save_state` 要 `state.year` 属性 → **AttributeError** | ✅ 实测 | 删 5 行，单行 `db.save_state(state)` |
| **P0-A2** | `models.py:64-88` | `Skill` 字段错位：`(sid, name, cost, effect, unlock_level, tier)` 但调用 `Skill("jx_01", "轻赋薄敛", "田赋收入+15%", 0, 1, "经略", …)` → unlock_level=0、tier=1 **全部错位** | ✅ 实测 8 处构造 | 调为 `(sid, name, effect, tier, unlock_level, branch, …)` |
| **P0-A3** | `db.py:962-979` `save_state` | `INSERT … ON CONFLICT(id)` 缺 PRIMARY KEY → 多行错乱 | ⚠️ 未实测 | 加 `id INTEGER PRIMARY KEY` DDL |
| **P0-A4** | `agents.py:243-377` 5 个 factory | `api_key=""` 而非 `_api_key` → 5 个 agent 连锁失败 | ✅ 实测 agents.py | 改 `os.environ.get("MINIMAX_API_KEY", "")` |
| **P0-A5** | `llm_config.py:50-80` | fallback 默认 DeepSeek → headless `getpass()` 100% SystemExit | ⚠️ 未实测 | 显式 raise RuntimeError |

**P0-A1 + A2 影响**：**所有月推演/存档实际必崩**，测试 83/83 通过只因没覆盖 simulation 主路径。**v2.0.0 必须修**。

### P0-B 前端致命问题（10 个）

| # | 位置 | 现象 | 修复 |
|---|------|------|------|
| **P0-B1** | `App.tsx:406` | `💾 存档` 按钮无 `onClick` → 死按钮 | 接 `saveGame` |
| **P0-B2** | `App.tsx:339, 370` | `onStageComplete={() => {}}` + `GrandMap provinces=[]` 死 prop | 删或接真逻辑 |
| **P0-B3** | `App.tsx:248` | `onRefresh` useEffect 依赖 → 死循环 | 修依赖 |
| **P0-B4** | `App.tsx:103-149` | EventSource SSE 无 ref + 无 cleanup → 内存泄漏 | useRef + cleanup |
| **P0-B5** | `MinisterPortrait.tsx:73` + `CharacterPortrait.tsx:28` | `CharacterPortrait` 同名 export 冲突 → 运行时不确定 | 合并 5 肖像组件，删 2 个死 export（9.1 KB） |
| **P0-B6** | `App.tsx:142, 351` | `cancelSecretOrder` 链式 `.then(r => setSecretOrders(r.orders))` 错引 `r.orders` | 改 `r.message` |
| **P0-B7** | `app.css:858` | 游离 `}` 单独成行 | 删 |
| **P0-B8** | `app.css:869-912` | `chinese-border` 死样式（grep 0 命中） | 删 |
| **P0-B9** | `index.html` | 无 `<link>` Google Fonts → Linux 玩家掉到 Times New Roman | 加 Noto Serif SC |
| **P0-B10** | `MinisterTab` + `CourtLayout` + `OrdersTab` | 3 处大臣/密令/朝会视图 markup 重复 | 抽共享组件 |

---

## 二、🟠 P1 重要修复（**12+8 个**，强烈建议）

### P1-A 后端（12 个，按 ROI 排序）

| # | 内容 | 工作量 |
|---|------|--------|
| P1-A1 | 4 个占位 tool 真接：`read_past_report` / `recall_memory_detail` / `activate_emperor_skill` / `issue_royal_decree` | 0.5d |
| P1-A2 | `agents.py:104` 重复定义 `run_agent_stream_text` → 删 1 个 | 0.1d |
| P1-A3 | `simulation.py:519-592` 4 处 `except: pass` 改 `logger.warning(exc_info=True)` | 0.2d |
| P1-A4 | 拆 `db.py` 2757 行 → `db/tables/` 6 文件 | 1d |
| P1-A5 | 拆 `flows.py` 1300 → `flows/economic.py` + `flows/drama.py` | 0.8d |
| P1-A6 | 拆 `issues.py` 1517 → `issues/{events,tracker,crisis}.py` | 0.8d |
| P1-A7 | 拆 `decree.py` 1037 → `decree/{models,lifecycle}.py` | 0.5d |
| P1-A8 | `apply_loyalty_decay` / `apply_warlord_loyalty_decay` / `apply_authority_decay` 抽公共 `apply_metric_decay` | 0.3d |
| P1-A9 | `agents.py` 5 个 factory 抽 `make_chat_agent(name, instructions, **kwargs)` | 0.2d |
| P1-A10 | `db.py:load_state_key` 重命名 `load_metric_value` | 0.05d |
| P1-A11 | `issues.py` `tracker_output` 未赋值 → 删或接 | 0.1d |
| P1-A12 | `seed_opening_crises` 加 `INSERT OR IGNORE` 幂等 | 0.1d |

**小计**：~4.6d

### P1-B 前端（8 个）

| # | 内容 | 工作量 |
|---|------|--------|
| P1-B1 | 全 emoji 替换为 lucide-react SVG（11 Tab 标签 + 8 Modal 按钮） | 0.5d |
| P1-B2 | `app.css` 大红/大金渐变收敛到 ≤ 2 处 | 0.3d |
| P1-B3 | 加 3 个 `@media` 断点（1920/1280/1024） | 0.2d |
| P1-B4 | 抽 `<BaseModal>` `<Button variant>` `<Icon>` 通用组件 | 0.5d |
| P1-B5 | 抽 `<SecretOrderList>` `<MinisterGrid>` 共享组件 | 0.4d |
| P1-B6 | `api.ts` 改 `import.meta.env.VITE_API_BASE` 替代 `localhost:5555` | 0.1d |
| P1-B7 | `useGame` 加 AbortController + 错误回滚 | 0.3d |
| P1-B8 | `EmperorPanel` 硬编码「建安年间」「中平元年」+ 假数据 `turnCount*2.5` → 动态查表 | 0.3d |

**小计**：~2.6d

---

## 三、🟡 P2 优化（**10+8 个**，可选）

### P2-A 后端（10）
- P2-A1: `db.py` N+1 查询改 JOIN（list_court / list_personnel）
- P2-A2: `tools.py` 内 `list_court` 等循环改批量 `list_character_status_map`
- P2-A3: `models.py` 11 档 AUTHORITY_LEVELS 合并死档位
- P2-A4: `db.py:1899-1908` `try: json.loads() except: pass` 加 `d[field] = []`
- P2-A5: `cli/terminal.py` 全量加 `encoding="utf-8"`
- P2-A6: `agents.py:60-63` 系统 prompt 用 `format()` 模板
- P2-A7: `agents.py:180-185` fallback 路径也调 `on_thinking("")`
- P2-A8: `decree.py:1-100` 撤诏工具接到 simulation
- P2-A9: 3 处重复 `_api_key = os.environ.get(...); load_llm_config(...)` 抽公共函数
- P2-A10: `llm_config.py:31-32` `is_minimax_base_url` 改常量 `MINIMAX_BASE = "api.minimaxi.com"`

### P2-B 前端（8）
- P2-B1: 抽 i18n 框架（先支持 zh/en）
- P2-B2: 删 `animations.css` 25+ 未用 keyframes（9 KB）
- P2-B3: 拆 `App.tsx` 906 行 → `tabs/` 目录
- P2-B4: `FactionTab` `MapTab` 接真实 API，删 mock
- P2-B5: `DecreeCard` meta 表与 `EdictModal` 合并
- P2-B6: `MinisterStats` 改 `extends &`
- P2-B7: `Consort` 统一 `canonical_name`
- P2-B8: `clsx` 替代模板字符串 className

---

## 四、东汉末年内容缺失（**3 大历史事件** + 8 系统）

### 历史事件（P1-A 之外，**必须补的 3 大事件**）

| 事件 | 当前 | 工作量 | 历史依据 |
|------|------|--------|----------|
| **190 诸侯讨董** | 只有 `trigger_dongzhuo_trap` 单条 | 0.5d | 袁绍号召 5 路响应 |
| **200 官渡之战** | 兵力调度 + 烧乌巢事件 | 0.8d | 曹操 vs 袁绍 |
| **208 赤壁之战** | 孙刘联盟 + 5 回合事件链 | 1d | 周瑜火攻 |
| **220 曹丕篡汉** | 几乎未实装 | 0.5d | 禅让流程 + 刘协封山阳公 |

**小计**：2.8d

### 玩法系统（补 8 个）

| 系统 | 缺失 | 工作量 |
|------|------|--------|
| 子嗣继承 | 玩家无子嗣线 | 0.5d |
| 战役独立子 UI | 攻/守/援/撤 4 指令 | 0.5d |
| 羁绊网（父子/仇敌） | 触发背叛/救驾 | 0.3d |
| 6 结局多线 | 当前只判胜利 | 0.5d |
| 大赦/罪己/封禅 | 双刃剑诏令 | 0.3d |
| 189 年 60+ 关键人物清单 | 缺何进/何太后/蔡邕/荀彧/郭嘉 | 0.3d |
| 州郡太守铨选 | 13 州治理 | 0.5d |
| 黄巾残部（6 系） | 张燕/张白骑 | 0.3d |

**小计**：3.2d

---

## 五、UI 美学重做（东汉末年墨色竹简 + 朱砂印章）

### 当前问题
- 5+ 肖像组件冗余、6 处 modal 重复
- 11 Tab 全 emoji 标签
- 20+ 大金/大红渐变
- 25+ 死动画（9 KB）
- "建安年间" / "中平元年" 硬编码
- 0 响应式 / 0 a11y

### v2.0.0 美学目标

| 元素 | 当前 | v2.0.0 |
|------|------|--------|
| Tab 标签 | 🏠 📜 💬 🏯 emoji | SVG 印章（lucide） |
| 主色 | 金渐变 + 红渐变 | 玄黑/朱红/古金 3 色，渐变 ≤ 2 处 |
| 字体 | 系统 serif 兜底 | Noto Serif SC (Google Fonts) |
| 边框 | 4 套不同实现 | 统一 token `--border-jianlv` |
| 术语 | 藩镇/汉室库（唐宋） | 州郡/少府（东汉） |
| 动画 | 25+ 死动画 | 保留 18 个（删 25 个） |
| 字号 | 不统一 | 8/10/12/14/16/20/24/32 token |
| 间距 | 不统一 | 4/8/12/16/24/32 token |

---

## 六、📋 实施计划（**v2.0.0 = 大修版**）

### Phase 1: P0 修复（**必修**）—— 0.7d

| Step | 内容 | 工作量 |
|------|------|--------|
| 1.1 | P0-A1 save_state 5 行 → 1 行 | 0.05d |
| 1.2 | P0-A2 Skill 字段顺序修正（48 技能） | 0.1d |
| 1.3 | P0-A3 DDL 主键修复 | 0.05d |
| 1.4 | P0-A4 5 agent factory 改 `_api_key` | 0.05d |
| 1.5 | P0-A5 fallback 改 raise RuntimeError | 0.05d |
| 1.6 | P0-B1~B10 前端 10 个修复 | 0.4d |

**小计**：0.7d

### Phase 2: P1 后端架构拆解（**强烈建议**）—— 4.6d

| Step | 内容 | 工作量 |
|------|------|--------|
| 2.1 | 4 个占位 tool 真接 | 0.5d |
| 2.2 | agents.py 5 factory 抽公共 | 0.2d |
| 2.3 | db.py 2757 行 → 6 文件 | 1d |
| 2.4 | flows.py 1300 → 2 文件 | 0.8d |
| 2.5 | issues.py 1517 → 3 文件 | 0.8d |
| 2.6 | decree.py 1037 → 2 文件 | 0.5d |
| 2.7 | apply_metric_decay 抽公共 | 0.3d |
| 2.8 | 修 logger 吞错 4 处 | 0.2d |
| 2.9 | issues.py tracker_output 处理 | 0.1d |
| 2.10 | seed_opening_crises 幂等 | 0.1d |
| 2.11 | `load_state_key` 重命名 | 0.05d |

**小计**：4.6d

### Phase 3: P1 前端 UI 系统重做（**强烈建议**）—— 2.6d

| Step | 内容 | 工作量 |
|------|------|--------|
| 3.1 | 抽 `<BaseModal>` `<Button variant>` `<Icon>` 通用组件 | 0.5d |
| 3.2 | 合并 5 肖像组件 → 1 个 `<Avatar>` | 0.3d |
| 3.3 | 全 emoji 替换为 lucide-react SVG | 0.5d |
| 3.4 | 渐变收敛到 ≤ 2 处 | 0.3d |
| 3.5 | 加 3 个 @media 断点 | 0.2d |
| 3.6 | 抽 `<SecretOrderList>` `<MinisterGrid>` | 0.4d |
| 3.7 | api.ts env var | 0.1d |
| 3.8 | useGame AbortController | 0.3d |
| 3.9 | EmperorPanel 动态年号 + 真实数据 | 0.3d |

**小计**：2.6d

### Phase 4: 东汉末年内容补完（**强烈建议**）—— 6d

| Step | 内容 | 工作量 |
|------|------|--------|
| 4.1 | 190 诸侯讨董 | 0.5d |
| 4.2 | 200 官渡之战 | 0.8d |
| 4.3 | 208 赤壁之战 | 1d |
| 4.4 | 220 曹丕篡汉 | 0.5d |
| 4.5 | 子嗣继承 | 0.5d |
| 4.6 | 战役独立子 UI | 0.5d |
| 4.7 | 羁绊网 | 0.3d |
| 4.8 | 6 结局多线 | 0.5d |
| 4.9 | 大赦/罪己/封禅 | 0.3d |
| 4.10 | 189 年 60+ 人物补全 | 0.3d |
| 4.11 | 州郡太守铨选 | 0.5d |
| 4.12 | 黄巾残部 | 0.3d |

**小计**：6d

### Phase 5: P2 优化（**可选**）—— 3d

| Step | 内容 | 工作量 |
|------|------|--------|
| 5.1 | N+1 查询改 JOIN | 0.3d |
| 5.2 | 抽 i18n 框架 | 0.5d |
| 5.3 | 删 25+ 死动画 | 0.2d |
| 5.4 | 拆 App.tsx → tabs/ 目录 | 0.5d |
| 5.5 | FactionTab/MapTab 接真实 API | 0.4d |
| 5.6 | MinisterStats extends & | 0.1d |
| 5.7 | Consort 统一 canonical_name | 0.1d |
| 5.8 | clsx 替代模板字符串 | 0.2d |
| 5.9 | 修 encoding utf-8 | 0.2d |
| 5.10 | 修 prompt 拼接 escape | 0.2d |
| 5.11 | P2 其余杂项 | 0.3d |

**小计**：3d

### 总工作量

| Phase | 内容 | 天数 |
|-------|------|------|
| Phase 1 | P0 修复 | 0.7d |
| Phase 2 | P1 后端拆解 | 4.6d |
| Phase 3 | P1 前端重做 | 2.6d |
| Phase 4 | 东汉末年内容 | 6d |
| Phase 5 | P2 优化 | 3d |
| **合计** | | **16.9d** |

---

## 七、🧪 测试与文档

| Step | 内容 | 工作量 |
|------|------|--------|
| 测试 | Phase 1-4 每完成一步即跑测试 83/83 | 含在每步 |
| e2e_test | 加 `test_save_state` / `test_skill_unlock` / `test_run_turn` 3 个新测试 | 0.3d |
| README.md | 全面更新 v2.0.0：功能/截图/架构/部署/开发指南 | 0.3d |
| CHANGELOG.md | 写 v2.0.0 大修章节 | 0.1d |
| 法正知识库 | projects 表 status → v2.0.0 + work_logs | 0.05d |
| Git 推送 | 拆 3-4 个 commit | 0.05d |

---

## 八、🎯 主公决策（**v2.0.0 范围**）

弟子建议**主公从 4 档中选 1 档**：

### 档 A：**最小 P0 修**（0.7d）
- 只修 P0-A1+A2+A3+A4+A5 + P0-B1~B10
- **优点**：v2.0.0 立刻出，不会崩
- **缺点**：架构残缺 + UI 丑仍存在
- **不补东汉末年事件**
- 推送：v2.0.0

### 档 B：**P0 + P1 后端 + 前端**（8d）
- Phase 1+2+3 全做
- **优点**：v2.0.0 架构清爽，UI 商业感足
- **缺点**：东汉末年内容缺口未补
- 推送：v2.0.0

### 档 C：**P0 + P1 + 东汉末年内容**（14d）
- Phase 1+2+3+4
- **优点**：v2.0.0 = 完整版（架构 + UI + 内容）
- **缺点**：时间长
- 推送：v2.0.0

### 档 D：**全做**（17d）
- Phase 1+2+3+4+5
- **优点**：v2.0.0 = 商业级
- **缺点**：最久

---

## 九、弟子建议

**档 B（8d）** —— **P0 + P1 后端架构 + P1 前端 UI 系统**。

理由：
- 档 A 太短，**P0 修完 UI 仍是"现代 Demo"**——不到 v2.0.0 标志
- 档 C / D 涉及 3 大历史事件（官渡/赤壁/曹丕篡汉）—— **内容量大，需要剧情/对话专门设计**，超出"代码审查大修"范畴，建议 v2.1.0
- 档 B 8 天能把代码质量从 3.5/10 拉到 7/10

**主公拍板**：A / B / C / D / 自定义？

**弟子严格执行**（吸取教训：方案前不靠记忆，先实测再决策）。
